export interface CardInfo {
    title: string;
    description: string;
    logo?: string;
    dates?: string;
    website?: string;
    expandedText?: string[];
    image?: string;
}

export interface Logo {
    description: string;
    url: string;
}
