export interface TileInfo {
    title: string;
    description: string;
    logo?: string;
    dates?: string;
    website?: string;
    expandedText: string[];
}

export interface Logo {
    description: string;
    url: string;
}
